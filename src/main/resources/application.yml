spring:
  datasource:
    username: root
    password: mysql@123!
    url: jdbc:mysql://localhost:3306/springsecuritydemo-roles
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      springframework:
        security: DEBUG